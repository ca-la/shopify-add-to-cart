{
  "name": "shopify-add-to-cart",
  "version": "1.0.1",
  "description": "",
  "main": "docs/index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "format": "prettier --write --single-quote add.ts",
    "clean": "rm docs/*",
    "build": "npm run clean && tsc add.ts --outDir docs -m umd",
    "test": "npm run build && prettier --single-quote -l add.ts"
  },
  "repository": {
    "type": "git",
    "url": "git@git"
  },
  "author": "CALA <devops@ca.la>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.15.2",
    "typescript": "^3.1.6"
  }
}
